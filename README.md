# BCAD
Bootstrap Comparison of Attractor Dimensions - a package to refute (false) causal relations between observables of dynamical systems. 

Run as (e.g.):
python main.py -M 3 -n 200

M = number of years to use in SSA window
n = number of bootstrap samples
